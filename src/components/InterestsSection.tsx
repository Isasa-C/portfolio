'use client';

export default function InterestsSection() {
  return (
    <section id="interests" className="scroll-mt-24 px-4 py-24 sm:px-6 lg:px-8">
      <div className="mx-auto flex max-w-7xl justify-center py-12 text-center sm:py-16">
        <h2
          className="text-[clamp(3rem,10vw,5.5rem)] font-semibold italic tracking-[-0.05em] text-[var(--accent-strong)]"
          style={{ fontFamily: 'Georgia, serif' }}
        >
          Thank You!
        </h2>
      </div>
    </section>
  );
}
